version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies"
      - npm install  # Example for a Node.js app
  build:
    commands:
      - echo "Building the application"
      - npm run build
artifacts:
  files:
    - '**/*'